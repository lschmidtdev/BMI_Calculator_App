import 'package:flutter/material.dart';


class CustomSpacer16 extends StatelessWidget {
  const CustomSpacer16({ Key? key, }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 16,
    );
  }
}